name: FSO PIPELINE test

on:
  push:
    branches: 
      - main

jobs: 
  hello_world_job:
    runs-on: ubuntu-latest
    steps: 
      - name: Just say hello world
        run: |
          echo "Hello World!"
      - name: List of Files 
        run: ls -tla
      - name: Current Time
        run: echo "Current Time ${date}"
  simple_deployment_pipeline:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Check Node version
        run: node --version
      - name: Check npm version
        run: npm --version
      - name: Install dependencies
        run: npm installname: FSO PIPELINE test

on:
  push:
    branches: 
      - main

jobs: 
  hello_world_job:
    runs-on: ubuntu-latest
    steps: 
      - name: Just say hello world
        run: |
          echo "Hello World!"
      - name: List of Files 
        run: ls -tla
      - name: Current Time
        run: echo "Current Time ${date}"
  simple_deployment_pipeline:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v6
        with:
          node-version: 24
      - name: Check Node version
        run: node --version
      - name: Check npm version
        run: npm --version
      - name: Install dependencies
        run: npm install
      - name: Check Style
        run: npm run eslint